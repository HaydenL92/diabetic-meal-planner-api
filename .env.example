# Default local SQLite (dev)
# DATABASE_URL=sqlite:///./diabetic.db

# Example Postgres URL (for v0.2+)
# For use with local Docker Postgres or a managed Postgres instance
DATABASE_URL=postgresql+psycopg2://diabetic_user:diabetic_password@localhost:5432/diabetic_db